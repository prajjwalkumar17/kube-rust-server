apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rust-server-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: rust.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: rust-server-service
            port:
              number: 80
